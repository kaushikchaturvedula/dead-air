# Python deps only. The Grafana MCP server (grafana/mcp-grafana) is a separate
# runtime dependency, run self-hosted via Docker -- see docker-compose.yml.
google-adk
google-genai
# Pinned: google-adk requires mcp>=1.24,<2. Bare `mcp` resolves to 2.0.0, which
# drops mcp.shared.session and breaks `from google.adk.tools.mcp_tool import
# McpToolset` -- the ADK swallows the ImportError, so the toolset just silently
# vanishes rather than failing loudly.
mcp>=1.24,<2
python-dotenv
httpx

# Reflexive layer: the agent exports its own traces to Grafana Cloud AI
# Observability (brief §1). opentelemetry-sdk already arrives transitively with
# google-adk, which emits OTel spans but ships no exporter -- this supplies the
# missing exporter. OpenTelemetry is vendor-neutral instrumentation, not an
# agent framework or a model, so it does not touch the Google-only runtime rule.
# Pinned: OTel has made breaking changes across majors, and a silently
# incompatible exporter disables the reflexive layer rather than failing loudly.
opentelemetry-exporter-otlp-proto-http>=1.40,<2

# Deterministic rung-resolution measurement (docs/vision-spike.md). Vision
# cannot detect ladder_mismatch; this is the code path that decides it.
#
# Pinned: numpy 2.x changed the C ABI and broke compiled dependents widely, and
# this measurement is the sole authority on ladder_mismatch -- it must not
# silently change behaviour under a transitive upgrade.
numpy>=1.26,<3
pillow>=10,<12
